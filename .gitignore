# ── 品牌资源 ────────────────────────────────────────────────
# brand/ 里是带商标的图（logo、应用图标），只在内部分支提交，
# 开源分支不包含这些文件。详见 README「开源版与内部版」一节。
/brand/

# ── 编辑器 / 操作系统 ───────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
Desktop.ini

# ── 依赖 / 日志 ─────────────────────────────────────────────
node_modules/
*.log

# ── 本地临时文件 ────────────────────────────────────────────
*.bak
_tmp/

# 注意：物料借还管理.html 是交付物本身，必须提交，别加进来。
# 测试产物（test/*.txt、test/shots/*.png）是验证证据，也一起提交。
